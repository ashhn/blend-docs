import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import {Checkbox} from "@blend-ui/core";

import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const checkboxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
};
console.log("AGRS ", checkboxArgTypes);

<Meta title="Core|Layout/Checkbox" component={Checkbox} />

# Checkbox

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Default" argTypes={checkboxArgTypes}>
    {(args) => <Checkbox />}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
