import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { Select, SearchSelect, Box, Label } from "demo-blend-ui/core";

<Meta title="Core/Select" component={Select} />

# Select

<Canvas withToolbar>
  <Story name="Default">
    {(args) => (
      <Box width="450px">
        <Label htmlFor="cabinClass">Cabin Class</Label>
        <Select
          {...args}
          id="cabinClass"
          name="cabinClass"
          defaultValue="Premium Economy"
        >
          <option>Economy</option>
          <option>Premium Economy</option>
          <option>Business</option>
          <option>First Class</option>
          <option>
            With a super long label that doesn't get clobbered by the chevron
          </option>
        </Select>
      </Box>
    )}
  </Story>
</Canvas>

# **Props**

<Props of={Select} />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
