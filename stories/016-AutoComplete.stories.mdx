import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { AutoComplete, Text } from "@blend-ui/core";
import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
  list,
  showList,
  activeItem,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const boxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
  list,
};
console.log("AGRS ", boxArgTypes);

<Meta title="Core/AutoComplete" component={AutoComplete} />

# AutoComplete

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Default" argTypes={boxArgTypes}>
    {(args) => <AutoComplete suggestions={list} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
