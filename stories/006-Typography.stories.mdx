import { Meta, Story, Preview, Canvas } from "@storybook/addon-docs/blocks";

import { Typography } from "@blend-ui/core";

<Meta title="CoreTypography" component={Typography} />

# Typography

<Canvas withToolbar>
  <Story name="Basic" argTypes={boxArgTypes}>
    {(args) => <Typography />}
  </Story>
</Canvas>
