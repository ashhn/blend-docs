import { Meta, Story, Preview, Canvas } from "@storybook/addon-docs/blocks";

import { Typography } from "demo-blend-ui/core";

<Meta title="Core/Typography" component={Typography} />

# Typography

<Canvas withToolbar>
  <Story name="Basic" argTypes={boxArgTypes}>
    {(args) => <Typography />}
  </Story>
</Canvas>
