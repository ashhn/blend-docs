import {
  Meta,
  Story,
  Canvas,
  Props,
  ArgTypes,
} from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";

import { Avatar } from "demo-blend-ui/core";
import { Badge } from "./shared/badge.js";

<Meta title="Core/Avatar" component={Avatar} decorators={[withKnobs]} />

# Avatar

We can even preview multiple stories in a block. This
gets rendered as a group, but defines individual stories
with unique URLs and isolated snapshot tests.

<Canvas>
  <Story name="Image Avatar">
    {(args) => (
      <Avatar
        {...args}
        src={"https://i.pravatar.cc/100"}
        alt={"photo"}
        width={100}
        effect={{ hover: { width: 110 } }}
        style={{
          margin: "15px",
          filter: "drop-shadow(0px 4px 8px rgba(91, 92, 91, 0.25))",
        }}
      />
    )}
  </Story>
</Canvas>

# **Props**

<Props of={Avatar} />

<Canvas>
  <Story name="Initials Avatar">
    {(args) => (
      <Avatar
        {...args}
        width={100}
        effect={{ hover: { width: 110, fontWeight: 600 } }}
        initials="JD"
      />
    )}
  </Story>
</Canvas>

<Canvas>
  <Story name="Avatar Interactive">
    {(args) => (
      <Avatar
        {...args}
        width={100}
        effect={{ hover: { width: 110, fontWeight: 600 } }}
        <!-- initials="JD" -->
      />
    )}
  </Story>
</Canvas>

<Canvas>
  <Story name="Avatar Notification">
    {(args) => (
      <>
        <Avatar {...args} width={24} initials="JD" />
        <Badge>5</Badge>
      </>
    )}
  </Story>
</Canvas>

## **Resources**

- [Styled-system API](https://styled-system.com/api)
