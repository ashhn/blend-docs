import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Avatar } from "@blend-ui/core";
import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const boxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
};
console.log("AGRS ", boxArgTypes);

<Meta title="Core|Layout/Avatar" component={Avatar} />

# Avatar

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Basic" argTypes={boxArgTypes}>
    {(args) => (
      <div>
        <Box>
          <Box p={3} color="white" bg="blue">
            Hello
          </Box>
          <Box p={3} mt={2} colorStyle="error">
            Hello
          </Box>
          <Box p={3} mt={2} color="error">
            Hello
          </Box>
          <Box p={3} mt={2} color="warning">
            Hello
          </Box>
        </Box>
        <Avatar />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
