import {
  Meta,
  Story,
  Preview,
  Canvas,
  Props,
} from "@storybook/addon-docs/blocks";

import { Cell, Grid } from "demo-blend-ui/core";

import { Space, Color, Border } from "./shared/styledSystem";

<Meta
  title="Core/Cell"
  component={Cell}
  parameters={{ options: { showPanel: true } }}
/>

# Cell

Simple styled label component that supports a number of the styled-system props.

<Canvas withToolbar>
  <Story name="Basic">
    {(args) => (
      <Grid {...args}>
        <Cell {...args} p={3} color="white" bg="blue">
          This is a Cell
        </Cell>
        <Cell {...args} p={3} color="white" bg="orange">
          This is a Cell
        </Cell>
      </Grid>
    )}
  </Story>
</Canvas>

## **Props**

<Props of={Cell} />

## **Resources**

- <Space />
- <Color />
- <Border />
