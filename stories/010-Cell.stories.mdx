import { Meta, Story, Preview, Canvas } from "@storybook/addon-docs/blocks";

import { CssCell as Cell } from "@blend-ui/css-grid";

import { Space, Color, Border } from "./shared/styledSystem";

<Meta
  title="Core/Cell"
  component={Cell}
  parameters={{ options: { showPanel: false } }}
/>

# Cell

Simple styled label component that supports a number of the styled-system props.

<Canvas withToolbar>
  <Story name="Basic">
    <Cell xs={6} p={3} color="white" bg="blue">
      Hello
    </Cell>
  </Story>
</Canvas>

# Props should be here...

<Props of={Label} />

## **Resources**

- <Space />
- <Color />
- <Border />
