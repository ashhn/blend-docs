import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Box, boxRef } from "demo-blend-ui/core";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const boxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
};
console.log("AGRS ", boxArgTypes);

<Meta title="Core/Box" component={Box} />

# Box

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Default" argTypes={boxArgTypes}>
    {(args) => (
      <Box
        {...args}
        width={[1 / 2]}
        height={"100px"}
        border={"1px solid"}
        borderRadius={4}
        p={2}
        ref={boxRef}
        foo={"bar"}
      >
        Something with Ref
      </Box>
    )}
  </Story>
</Canvas>

<Preview withToolbar>
  <Story name="Colors">
    {(args) => (
      <Box {...args}>
        <Box p={3} color="white" bg="blue">
          Hello
        </Box>
        <Box p={5} mt={2} color="primary">
          Hello
        </Box>
        <Box p={3} mt={2} color="error">
          Hello
        </Box>
        <Box p={3} mt={2} color="warning">
          Hello
        </Box>
      </Box>
    )}
  </Story>
</Preview>

<ArgsTable story="Default" />

<Story name="Box Interactive">
  {(args) => (
    <Box size={100} border={"1px solid"} p={5} mt={6} {...args}>
      Interactive
    </Box>
  )}
</Story>

## **Resources**

- [Styled-system API](https://styled-system.com/api)
