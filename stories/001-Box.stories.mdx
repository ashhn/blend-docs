import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Box, boxRef } from "demo-blend-ui/core";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import { colorArgType } from "./shared/themeProps";

<Meta title="Core/Box" component={Box} />

# Box

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Box Interactive" argTypes={colorArgType}>
    {(args) => (
      <Box
        {...args}
        width={[1 / 2]}
        height={"100px"}
        border={"1px solid"}
        borderRadius={4}
        p={2}
        ref={boxRef}
        foo={"bar"}
      >
        Something with Ref
      </Box>
    )}
  </Story>
</Canvas>

<Preview withToolbar>
  <Story name="Box Stack">
    {(args) => (
      <Box {...args}>
        <Box p={3} color="white" bg="blue">
          Hello
        </Box>
        <Box p={5} mt={2} color="primary">
          Hello
        </Box>
        <Box p={3} mt={2} color="error">
          Hello
        </Box>
        <Box p={3} mt={2} color="warning">
          Hello
        </Box>
      </Box>
    )}
  </Story>
</Preview>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
