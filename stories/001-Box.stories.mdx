import {
  Meta,
  Story,
  Canvas,
  Preview,
  ArgsTable,
} from "@storybook/addon-docs/blocks";

import { Knobs } from "@storybook/addon-docs/knobs";

import { Box } from "@blend-ui/core";
import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const boxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
};

<Meta
  title="Core|Layout/Box"
  component={Box}
  parameters={{ options: { showPanel: false } }}
/>

# Box

A low-level layout component for setting width, margin, padding, and color

<Preview withToolbar>
  <Story name="Default" argTypes={boxArgTypes}>
    {(args) => (<Box/>)}
     <Box
    width={[1 / 2]}
    height={"100px"}
    border={"1px solid"}
    borderRadius={4}
    p={2}
    ref={boxRef}
    foo={"bar"}
  >
    Something with Ref
  </Box>
  </Story>
<Preview withToolbar>
<Knobs>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
