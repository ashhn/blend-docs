import {
  Meta,
  Story,
  Canvas,
  Props,
  argTypes,
} from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import { Checkbox } from "demo-blend-ui/core";
import { checkAction, CheckboxStateful } from "./shared/checkbox";
import { checkedStatus } from "./shared/checkbox";

import { color2ArgType } from "./shared/themeProps";

<Meta title="Core/Checkbox" component={Checkbox} />

# Checkbox

<Canvas withToolbar>
  <Story name="Default" argTypes={color2ArgType}>
    {(args) => (
      <Checkbox
        onChange={checkAction}
        id="checkbox"
        checked={!checkedStatus}
        {...args}
      >
        Unchecked by default
      </Checkbox>
    )}
  </Story>
</Canvas>

# **Props**

<Props of={Checkbox} />

<Story name="Checkbox Interactive" argTypes={color2ArgType}>
  {(args) => <CheckboxStateful {...args}>Press Me!</CheckboxStateful>}
</Story>

## **Resources**

- [Styled-system API](https://styled-system.com/api)
