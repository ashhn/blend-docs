import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import { Checkbox } from "demo-blend-ui/core";
import { checkAction, CheckboxStateful } from "./shared/checkbox";
import { checkedStatus } from "./shared/checkbox";

<Meta title="Core/Checkbox" component={Checkbox} />

# Checkbox

<Canvas withToolbar>
  <Story name="Default">
    <Checkbox onChange={checkAction} id="checkbox" checked={!checkedStatus}>
      Unchecked by default
    </Checkbox>
  </Story>
</Canvas>

# **Props**

<Props of={Checkbox} />

<Canvas withToolbar>
  <Story name="Stateful">
    <CheckboxStateful />
  </Story>
</Canvas>

<Story name="Checkbox Knobs" parameters={{ decorators: [withKnobs] }}>
  <CheckboxStateful
    onChange={boolean("checked", true)}
    id="checkbox"
    checked={boolean("sdada", checkedStatus)}
  >
    Press Me!
  </CheckboxStateful>
</Story>

<Canvas withToolbar>
  <Story name="Checkbox Controls">
    {(args) => <Checkbox {...args}>Check out Controls</Checkbox>}
  </Story>
</Canvas>

## **Resources**

- [Styled-system API](https://styled-system.com/api)
