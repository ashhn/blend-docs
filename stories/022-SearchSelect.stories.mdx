import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Select, SearchSelect, Box, Label } from "demo-blend-ui/core";

import {
  SpaceArgType,
  LayoutArgType,
  ColorArgType,
  BorderArgType,
  boxRef,
  selectOptions,
  changeAction,
} from "./shared/styledProps";
import { ColorStylesArgType } from "./shared/themeProps";

export const boxArgTypes = {
  ...ColorStylesArgType,
  ...SpaceArgType,
  ...LayoutArgType,
};
console.log("AGRS ", boxArgTypes);

<Meta title="Core/SearchSelect" component={SearchSelect} />

# Select

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Default" argTypes={boxArgTypes}>
    {(args) => (
      <Box>
        <Label htmlFor="cabinClass">Cabin Class</Label>
        <SearchSelect id="cabinClass" name="cabinClass" />
      </Box>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## **Resources**

- [Styled-system API](https://styled-system.com/api)
