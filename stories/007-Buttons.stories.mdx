import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import { Button, colors } from "demo-blend-ui/core";
import { BlendIcon, eyeIcon } from "@blend-ui/icons";

<Meta
  title="Core/Button"
  component={Button}
  parameters={{
    options: { showPanel: true },
    decorators: [withKnobs],
  }}
/>

export const Template = (args) => <Button {...args}>Default</Button>;

# Button

<Preview withToolbar>
  <Story name="Default">{Template.bind({})}</Story>
</Preview>

<Props of={Button} />

<Preview withToolbar>
  <Story name="Outline">
    {(args) => (
      <Button {...args} variation={"outline"}>
        Outline
      </Button>
    )}
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="Disabled">
    <Button disabled>Disabled</Button>
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="Button Link">
    <Button variation={"link"}>Something</Button>
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="Disabled Button Link">
    <Button disabled variation={"link"}>
      Something
    </Button>
  </Story>
</Preview>

### Variations

<Story name="Size Variations">
  <div
    style={{
      height: 100,
      width: 600,
      flexDirection: "row",
      justifyContent: "space-between",
    }}
  >
    <Button size="lg">Large</Button>
    <Button size="md">Medium</Button>
    <Button size="sm">Small</Button>
    <Button size="xs">Extra small</Button>
  </div>
</Story>

<Story name="State Variations">
  <div
    style={{
      height: 100,
      width: 600,
      flexDirection: "row",
      justifyContent: "space-between",
    }}
  >
    <Button>Default</Button>
    <Button
      style={{ backgroundColor: colors.baseHover, border: colors.baseHover }}
    >
      Hover
    </Button>
    <Button disabled>Disabled</Button>
  </div>
</Story>

<Story name="Style Variations">
  <div
    style={{
      height: 100,
      width: 60,
      flexDirection: "row",
      justifyContent: "space-between",
    }}
  >
    <Button variation={"fill"}>Fill</Button>
    <Button variation={"outline"}>Outline</Button>
    <Button variation={"link"}>Link</Button>
    <Button variation={"file"}>File</Button>
  </div>
</Story>

<Story name="Color Variations">
  <div>
    <Button variation={"fill"} colorStyle={"secondary"}>
      Default
    </Button>
    <Button variation={"fill"} colorStyle={"error"}>
      Default
    </Button>
    <Button variation={"outline"} colorStyle={"secondary"}>
      Outline
    </Button>
    <Button variation={"outline"} colorStyle={"error"}>
      Outline
    </Button>
  </div>
</Story>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="Button Knobs" parameters={{ decorators: [withKnobs] }}>
  <Button />
</Story>
