import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, boolean } from "@storybook/addon-knobs";
import { variations, sizes, colors } from "./shared/buttons";
import { Button, Box } from "@blend-ui/core";

<Meta
  title="Button"
  component={Button}
  parameters={{ options: { showPanel: true } }}
/>

# Button

<Preview withToolbar>
  <Story name="Standard">
    <Button variation={"fill"} size={"md"} color={"primary"}>
      BUTTON
    </Button>
  </Story>
</Preview>

<Props of={Button} />

### Variations

<Story name="Button Variations">
  <Button variation={"fill"} size={"sm"} color={"primary"}>
    BUTTON
  </Button>
  <Button variation={"outline"} size={"lg"} color={"primary"}>
    BUTTON
  </Button>
  <Button variation={"link"} size={"lg"} color={"primary"}>
    BUTTON
  </Button>
</Story>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="Button Knobs" parameters={{ decorators: [withKnobs] }}>
  <Button
    <!-- fullWidth={boolean("Full Width?", false)} -->
    disabled={boolean("Disabled?", false)}
    variation={select("Variation", variations, "fill")}
    size={select("Size", sizes, "md")}
    color={select("Color", colors, "primary")}
  >
    Button
  </Button>
</Story>
