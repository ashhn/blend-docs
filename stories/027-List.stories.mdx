import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";

import { List, ListDivider, ListItem } from "demo-blend-ui/list-item";

import { action } from "@storybook/addon-actions";
import plusCircle from "@iconify/icons-mdi/plus-circle";

<Meta
  title="List/List"
  component={List}
  subcomponent={ListDivider}
  decorators={[withKnobs]}
/>

# List

We can even preview multiple stories in a block. This
gets rendered as a group, but defines individual stories
with unique URLs and isolated snapshot tests.

<Canvas>
  <Story name="List">
    {(args) => (
      <div>
        <List spacing={3} {...args}>
          <ListItem onClick={action("Clicked IconButton")}>
            List Item One
          </ListItem>
          <ListDivider />
          <ListItem>List Item Two</ListItem>
          <ListDivider />
          <ListItem variation={"link"} href="#">
            List Item Link
          </ListItem>
          <ListDivider />
          <ListItem variation={"icon"} iconName="smileAltIcon">
            List Item Icon
          </ListItem>
          <ListDivider />
          <ListItem variation={"icon"} iconify={plusCircle} iconColor="red">
            List Item Icon
          </ListItem>
          <ListDivider />
          <ListItem
            variation={"icon-link"}
            iconify={plusCircle}
            iconColor="red"
            href="#"
          >
            List Item Icon Link
          </ListItem>
          <ListDivider />
          <ListItem variation={"icon-link"} iconName="smileAltIcon" href="#">
            List Item Icon Link
          </ListItem>
        </List>
      </div>
    )}
  </Story>
</Canvas>

# **Props**

<Props of={List} />
