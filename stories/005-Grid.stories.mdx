import {
  Meta,
  Story,
  Props,
  Preview,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import { Space, Color, Layout, Border } from "./shared/styledSystem";

import { Grid, Cell, Flex, Text } from "demo-blend-ui/core";

import { bgArgTypes } from "./shared/themeProps";

<Meta
  title="Core/Grid"
  component={Grid}
  parameters={{ options: { showPanel: true } }}
/>

# Grid

A low-level grid layout component for setting width, margin, padding, and color

<Canvas>
  <Story name="Grid Interactive" argTypes={bgArgTypes}>
    {(args) => (
      <Grid {...args}>
        <Cell {...args}>
          <Flex
            {...args}
            p={2}
            border={"2px solid green"}
            borderRadius={4}
            width="200px"
            bg="yellow"
          >
            <Text>1</Text>
          </Flex>
        </Cell>
        <Cell {...args}>
          <Flex
            {...args}
            p={2}
            border={"2px solid blue"}
            borderRadius={4}
            width="200px"
            bg="red"
          >
            <Text>2</Text>
          </Flex>
        </Cell>
      </Grid>
    )}
  </Story>
</Canvas>

<Props of={Grid} />

### Variations

<Canvas>
  <Story name="Grid Variations">
    <Grid direction="column" justify="center" minHeight="100vh">
      <Cell xs={12} textAlign="center" justify="center">
        <Flex p={2} border={"2px solid"} borderRadius={4} width="200px">
          <Text>Center this</Text>
        </Flex>
      </Cell>
    </Grid>
    <Grid direction={"column"}>
      <Cell xs={3}>Cell 1</Cell>
      <Cell xs={6}>Cell 2</Cell>
    </Grid>
  </Story>
</Canvas>
