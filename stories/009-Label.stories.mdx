import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Label } from "demo-blend-ui/core";
import { Space, Color, Border } from "./shared/styledSystem";

<Meta
  title="Core/Label"
  component={Label}
  parameters={{ options: { showPanel: true } }}
/>

# Label

Simple styled label component that supports a number of the styled-system props.

<Preview withToolbar>
  <Story name="Default">{(args) => <Label m={3}>Label Component</Label>}</Story>
</Preview>

<Props of={Label} />

## **Resources**

- <Space />
- <Color />
- <Border />
