import { Meta, Story, Canvas, Props } from "@storybook/addon-docs/blocks";
import { IconField, Box } from "demo-blend-ui/core";

import { BlendIcon } from "demo-blend-ui/icons";

import bxUser from "@iconify/icons-bx/bx-user";
import bxLock from "@iconify/icons-bx/bx-lock";
import hidePassword from "@iconify/icons-mdi/keyboard-hide";
import eyeIcon from "@iconify/icons-mdi/monitor-eye";
import onHide from "@iconify/icons-mdi/hide";

<Meta title="Core/IconField" component={IconField} />

# IconField

A low-level layout component for setting width, margin, padding, and color

<Canvas withToolbar>
  <Story name="Default">
    {(args) => (
      <IconField {...args}>
        <IconField.LeftIcon
          iconify={bxUser}
          color={"componentPrimary"}
          size={"17"}
        />
        <IconField.InputField placeholder={"Enter value here"} />
      </IconField>
    )}
  </Story>
</Canvas>

# **Props**

<Props of={IconField} />

### Variations

<Story name="Input Variations">
  <IconField>
    <IconField.LeftIcon
      iconify={bxLock}
      color={"componentPrimary"}
      size={"17"}
    />
    <IconField.InputField placeholder={"Enter value here"} />
    <Box display={"inline-flex"} onClick={onHide}>
      <IconField.RightIcon
        iconify={hidePassword ? eyeIcon : bxHide}
        color={"componentPrimary"}
        size={"17"}
      />
    </Box>
  </IconField>
</Story>

## **Resources**

- [Styled-system API](https://styled-system.com/api)
