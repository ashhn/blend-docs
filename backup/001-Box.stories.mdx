import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs/blocks";
import { Box } from "@blend-ui/core";
import { Space, Color, Layout, Border } from "./shared/styledSystem";

export const buttonArgTypes = {
  colorStyle: {
    name: "Changed prop",
    description: "Color style from theme",
    control: {
      type: null,
    },
  },
  space: {
    name: "space",
    description:
      "Styled systems space props - [Styled-system space](https://styled-system.com/api#space)",
    type: {
      name: "string",
    },
    control: {
      type: null,
    },
  },
  buttontype: {
    description: "Button Type",
    table: {
      type: {
        summary: "something short",
        detail: "something really really long",
      },
    },
  },
};

<Meta title="Core/Layout/Box" component={Box} />

# Box

A low-level layout component for setting width, margin, padding, and color

<Preview withToolbar>
  <Story name="Default" argTypes={buttonArgTypes}>
    {(args) => (
      <Box>
        <Box p={3} color="white" bg="blue">
          Hello
        </Box>
        <Box p={3} mt={2} color="primary">
          Hello
        </Box>
        <Box p={3} mt={2} color="error">
          Hello
        </Box>
        <Box p={3} mt={2} color="warning">
          Hello
        </Box>
      </Box>
    )}
  </Story>
</Preview>
<ArgsTable story="Default" />
## **Resources**

- <Space />
- <Layout />
- <Color />
- <Border />

[Styled-system space](https://styled-system.com/api#space "_blank")
